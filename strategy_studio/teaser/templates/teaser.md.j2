# {{ company_short }} — Strategy Teaser

**For {{ contact_name }}, {{ contact_role }}**

{{ industry }} · {{ employees }} employees · ${{ '%.0f'|format(revenue_usd_m) }}M · {{ years_in_business }} years · {{ headquarters }}

---

## You are {{ wound_months }} months from being locked out of the {{ wound_channel }}.

**Trigger:** {{ wound_trigger }}

---

## What we see

**{{ capability_count }} capabilities. {{ capability_gap }}**

{% for cap in capability_names %}
- **{{ cap }}** — defensible, not yet productized as intelligence
{% endfor %}

---

## The mechanism we'd build: {{ mechanism_name }}

{{ mechanism_description }}

---

## Three structural advantages you don't see in yourself

{% for adv in advantages %}{{ loop.index }}. {{ adv }}
{% endfor %}

---

## The analog that already happened: {{ comparable_company }}

- **{{ comparable_year_start }}:** ${{ '%.0f'|format(comparable_revenue_start_m) }}M revenue, zero new segment.
- **{{ comparable_year_end }}:** ${{ '%.0f'|format(comparable_revenue_end_m) }}M revenue, new segment grew to **${{ '%.0f'|format(comparable_segment_growth_m) }}M**.

The pivot {{ comparable_company }} made is the pivot in front of {{ company_short }} today.

---

## Three category engines

| Engine | Type | Loop | Target |
|---|---|---|---|
{% for e in engines %}| **{{ e.name }}** ({{ e.sigma_label }}) | {{ e.flywheel_type }} flywheel | {{ e.flywheel_loop }} | ${{ '%.0f'|format(e.target_revenue_m) }}M |
{% endfor %}

---

## Who arrives first if you don't move

| Threat | Tier | Horizon | Key fact | SW |
|---|---|---|---|---|
{% for t in threats %}| **{{ t.name }}** | {{ t.tier }} | {{ t.horizon_months }}mo | {{ t.key_fact }} | {{ '%.2f'|format(t.source_weight) }} |
{% endfor %}

---

## Three engagement tiers

| Tier | Length | Investment | What you get |
|---|---|---|---|
| **Diagnostic** | 2 weeks | $14K fixed | One-page memo + 3 disqualifier tests. Falsification gate. GO/NO-GO. |
| **Activation** | 3 months | $95K + retainer | Foundation + Solo deploy + first production workflow. Day-90 ROI. |
| **Transformation** | 9–18 months | $185K–$485K + retainer | Full Forge-pattern: Solo, Team, Vault, Edge, Sight, Blueprint. Production guarantee or prorated refund. |

---

## When we say no — three disqualifiers

{% for dq in disqualifiers %}- {{ dq }}
{% endfor %}

---

## What happens next

If anything in this memo is wrong, we want to know first. Send the falsification.

No calendar link. No "happy to chat". {{ contact_name }} replies to **strategy@rodgers-intelligence.com** with the line in this memo that's wrong, or the wound number that's off.

---

**Confidence: {{ confidence }}**

**Sources:** {% for src in evidence_sources %}{{ src }}{% if not loop.last %} · {% endif %}{% endfor %}

Cloned site: {{ cloned_site_url }}

_Generated by RIG Strategy Studio · A1 frozen pipeline · ProofPacket on file._
